// dynamic memory allocation : it is a way to a data structure during the runtime.
// we need some functions to allocate and free memory dynamically.
// function of DMA : 
// malloc() = memory allocation
// calloc() = contineaous allocation
// free()
// realloc() = re allocation
// malloc()
// memory allocation
// takes number of bytes to be allocated and returns a pointer of type void.
// ptr = (int*)malloc(5*sizeof(int));
#include<stdio.h>
int main(){
    // printf("%d\n",sizeof(int));
    // printf("%d\n",sizeof(float));
    // printf("%d\n",sizeof(char));

return 0;
}